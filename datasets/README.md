# datasets/

Recorded teleop demonstrations live here. **Not committed to git.**

Suggested layout:
```
datasets/
└── <task_name>/
    └── episode_<NNNN>/   (LeRobot dataset format)
```

Sync between machines via rsync / HuggingFace Hub / external drive.
